name: CI
on: push

jobs:
  linux:
    name: Linux build on node ${{ matrix.node_version }}
    runs-on: ubuntu-latest
    steps:
    - name: chmod
      run: chmod +x ./build-powerbox.sh
    - name: build
      run: ./build-powerbox.sh"
